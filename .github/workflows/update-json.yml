name: Update Cybersecurity JSON
on:
  schedule:
    - cron: '0 9 * * 1' # Monday 9 AM UTC
  workflow_dispatch:
jobs:
  update-json:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
    - uses: actions/checkout@v3
    - uses: actions/setup-python@v4
      with:
        python-version: '3.x'
    - run: |
        python -m pip install --upgrade pip
        pip install feedparser requests python-dateutil pytz
    - run: python sec_dashboard.py
    - run: python render_html.py
    - run: |
        git config --global user.name 'GitHub Action'
        git config --global user.email 'action@github.com'
        git add cybersecurity_data.json cache.json dashboard.html
        git commit -m "Update cybersecurity JSON and dashboard HTML" || echo "No changes"
        git push